name: CodeQL

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

permissions:
  contents: read
  security-events: write

jobs:
  codeql:
    name: CodeQL / codeql
    runs-on: ubuntu-latest
    steps:
      - run: echo "noop codeql"
